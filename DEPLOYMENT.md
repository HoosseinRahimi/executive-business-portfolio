# Deployment

This repository includes a GitHub Pages workflow at `.github/workflows/pages.yml`.

If the repository has not used Pages before, select **Settings → Pages → Build and deployment → Source → GitHub Actions** once. After that, every push to `main` deploys the site automatically.
